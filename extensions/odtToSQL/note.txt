This class will convert export databases like "odt" to SQL (or import directly to database).

It will be usefull if you do a backup from php my admin "odt" for example, it can't read this tipe of file.